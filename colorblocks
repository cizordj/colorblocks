#!/bin/sh
cd "$(dirname "$(realpath "$0")")" || exit 1

mkdir -p "$HOME/.cache/colorblocks/rofi"
cp ./scripts/rofi_template/* "$HOME/.cache/colorblocks/rofi"

polybar-msg cmd quit
polybar -q main -c config.ini &
